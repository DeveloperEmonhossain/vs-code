#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> v(n);

        for (int i = 1; i <= n; i++)
        {
            int x;
            cin >> x;
            v[i] = x;
        }

        int cnt = 0, sum = 0;
        vector<int> sub(n);
        for (int i = 1; i <= n; i++)
        {
            if (v[i] > 0)
            {
                if (v[i] > 0)
                {
                    cnt++;
                    sub[i] = v[i];
                }
                else
                {
                    cnt++;
                    int copy=v[]
                }
            }
        }
    }
}
}